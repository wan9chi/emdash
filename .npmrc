# Temporary: test a Vite+ preview build from the registry bridge, which serves
# 0.0.0-commit.<sha> builds and proxies every other package to npm. Revert this
# file and the `vite-plus` pin in package.json once the change ships.
# https://viteplus.dev/guide/upgrade#preview-builds
registry=https://registry-bridge.viteplus.dev/
